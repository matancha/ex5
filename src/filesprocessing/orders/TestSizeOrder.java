/*
package filesprocessing.orders;
import org.junit.*;

import java.util.*;
import java.io.*;

public class TestSizeOrder {
	String filesDir = "C:/Users/Matan/IdeaProjects/ex5/Files";


	@Test
	public void orderBySize() {
		List<File> filesArray = Arrays.asList(new File(filesDir + "/test.txt"),
				new File(filesDir + "/test1.txt"));
		List<File> orderedFiles = new SizeOrder().getFilesInOrder(new ListArray<File>());

	}
}
*/
